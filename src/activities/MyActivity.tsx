import { AppScreen } from '@stackflow/plugin-basic-ui';

function MyActivity() {
  return (
    <AppScreen
      appBar={{
        title: 'MyActivity'
      }}
    >
      반가워요!
    </AppScreen>
  );
}

export default MyActivity;
